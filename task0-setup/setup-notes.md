# Task 0 — Environment Setup

## Commands Used

```bash
mkdir -p ~/backend-practice
cd ~/backend-practice
python3 -m venv .venv
source .venv/bin/activate
python --version
sqlite3 practice.db ".databases"
